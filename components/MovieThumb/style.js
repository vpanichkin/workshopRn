
import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    borderColor: 'blue',
    marginVertical: 15,
    borderRadius: 6,
  },
  textBox: {
    fontSize: 20,
  }
});
